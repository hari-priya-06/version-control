Introduction to Version Control with Git

Name:Malladi Gayathri Haripriya

Task Overview


This task is about learning the fundamentals of version control using Git.

In this task, I created a local Git repository, added files, committed changes, created a branch, merged the branch, and pushed the final project to GitHub.

Git Concepts Practiced


Git initialization
Checking Git status
Adding files to staging area
Committing changes
Creating branches
Switching branches
Merging branches
Connecting local repository to GitHub
Pushing code to GitHub

Commands Used


git init

git status

git add .

git commit -m "message"

git branch

git checkout -b branch-name

git merge branch-name

git remote add origin repository-url

git push -u origin main

Branching Practice
I created a separate branch named feature/git-basics to understand how branching works in Git.

After making changes in this branch, I merged it back into the main branch.

This helped me understand how developers work on separate features without directly changing the main branch.
